# -*- coding: utf-8 -*-
"""{概念名} 示例脚本
所属章节：{NN-章节名}
运行方式：python "{概念名}-示例.py"（本机建议加 -X utf8 参数）
仅使用 Python 标准库演示；确需 numpy/pandas 时在此说明原因。
"""

# --------------------------------------------------
# 1. 构造（或从 datasets/ 读取）示例数据
# --------------------------------------------------
data = [...]  # TODO: 示例数据；超过 20 条时改为读取 datasets/ 下的 CSV


# --------------------------------------------------
# 2. 核心计算：{概念名}
# --------------------------------------------------
def demonstrate(data):
    """演示 {概念名} 的计算过程，逐步打印中间结果。"""
    # TODO: 逐步计算并打印，让读者能跟随每一步
    ...


# --------------------------------------------------
# 3. 运行与输出
# --------------------------------------------------
if __name__ == "__main__":
    demonstrate(data)
    # 输出会被复制进笔记的「Python 实战」一节，保持整洁、有标签
